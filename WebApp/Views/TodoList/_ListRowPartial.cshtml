@using WebApp.Models.TodoList
@model TodoListModel

<tr>

	<td>@Model.Title</td>
	<td>@Model.Description</td>

	<td>
		<a asp-action="Details" asp-route-id="@Model.Id">See details</a>
	</td>
</tr>

@* <tr>
	<td colspan="3">
		@foreach (var tag in Model.Tags)
		{
			<span>@tag.Title</span>
		}
	</td>
</tr> *@